import 'package:flutter/material.dart';

class BroadScreen extends StatelessWidget {
  const BroadScreen({Key? key}) : super(key: key);
  static const routeName = '/broad_screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text("BroadScreen"),
    );
  }
}
